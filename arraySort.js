const fruits = ["Banana", "Orange", "Apple", "Mango"];

fruits.sort();  // also toSorted
fruits.reverse(); // also toReversed
console.log(fruits)
// console.log(months)

const a = [1,20,3];
const b = a.sort((a, b) => a - b); // sort in ascending order
console.log(b)
